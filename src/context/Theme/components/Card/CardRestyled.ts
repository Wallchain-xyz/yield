import { MUIRestyled } from '../MUIRestyled';

export const CardRestyled: MUIRestyled<'MuiCard'> = {
  key: 'MuiCard',
  common: {
    styleOverrides: {
      root: {
        boxShadow: 'none',
      },
    },
  },
};
